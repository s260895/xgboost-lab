# Data + splitting config for California Housing (Stage 2 regression).
# Loaded into xgblearn.config.DataConfig.

dataset: california_housing
seed: 42

test_size: 0.2
val_size: 0.2
stratify: false           # regression target is continuous — no stratification

raw_dir: data/raw
