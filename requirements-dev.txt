# Development dependencies (linting, typing, testing, notebooks).
# Install alongside runtime deps:
#   pip install -r requirements.txt -r requirements-dev.txt

ruff>=0.6          # lint + format
mypy>=1.11         # static type checking
pytest>=8.0
pytest-cov>=5.0
pre-commit>=3.8
ipykernel>=6.29    # Jupyter kernel for the .venv
jupyterlab>=4.2
